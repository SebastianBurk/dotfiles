;;; early-init.el --- TEXT -*- lexical-binding: t -*-

;;; Commentary:

;;
;;
;;

;;; Code:

(setq package-enable-at-startup nil)

;; So we can detect this having been loaded
(provide 'early-init)

;;; early-init.el ends here
